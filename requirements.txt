# 项目管理 MCP 应用依赖
mcp>=1.0.0
aiohttp>=3.8.0
requests>=2.28.0
loguru>=0.7.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# WebSocket 客户端（与 MCP 同时启动时的后端监听）
websockets>=12.0

# 本地 Cursor 控制（WebSocket 收到命令时调用）
pywinauto>=0.6.8
pyautogui>=0.9.54
pyperclip>=1.8.2
